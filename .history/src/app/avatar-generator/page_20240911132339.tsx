import AvatarGenerator from '../../components/AvatarGenerator';

export default function AvatarGeneratorPage() {
  return (
    <div className="h-screen w-screen overflow-hidden bg-gradient-to-br from-pink-400 to-purple-600">
      <AvatarGenerator />
    </div>
  );
}