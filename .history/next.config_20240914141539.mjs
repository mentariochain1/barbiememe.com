/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  // basePath: '/путь_к_вашему_сайту', // Закомментируйте или удалите эту строку, если загружаете в корень
};

export default nextConfig;
