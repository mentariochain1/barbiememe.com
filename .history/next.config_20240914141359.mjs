/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/путь_к_вашему_сайту', // Например, '/my-site' если сайт размещен в подпапке
};

export default nextConfig;
